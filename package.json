{
  "name": "form-html",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "dev": "npm run clean && parcel src/index.html --out-dir development -p 3000",
    "build": "parcel build src/*.html --out-dir dist --src-url ./",
    "clean": "rimraf ./development && rimraf -rf ./.cache"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel-bundler": "^1.12.4"
  }
}
